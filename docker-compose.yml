services:
  bot:
    build: .
    container_name: telegram-ticker-bot
    env_file:
      - .env
    restart: unless-stopped
    mem_limit: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
